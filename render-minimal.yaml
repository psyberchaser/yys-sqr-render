services:
  - type: web
    name: yys-sqr-minimal
    env: python
    buildCommand: pip install -r requirements-minimal.txt
    startCommand: python minimal_server.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /api/health